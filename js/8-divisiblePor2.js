const numero= parseInt(prompt("Ingrese un numero"))
if(numero % 2 === 0){
    document.writeln(`El numero ${numero} es divisible por dos`)
}else{
    document.writeln(`El numero ${numero} no es divisible por dos`)
}
